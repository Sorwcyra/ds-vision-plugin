# Security policy

Report vulnerabilities privately to the repository owner before opening a public issue. Do not include API keys, private images, provider responses, or full configuration files in reports.

Cloud channels receive the bytes of the selected file. Restrict `allowedRoots`, keep keys in environment variables, prefer local OCR/VLMs for sensitive data, and pin plugin revisions before team deployment.
